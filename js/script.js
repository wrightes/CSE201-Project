/* This is the js file which will contain javascript functions to sort, filter, and search apps */

// At the meeting will get clarification about how they want each of these requirements to
// work and what the parameters should be.

// returns a table sorted by names in alphabetical order
sortNames {
	
}

// returns a table sorted by developers in alphabetical order
sortDev {
	
}

// returns a table sorted by most platforms to lowest
sortPlatforms { 

}

// returns a table sorted by highest price to lowest
sortPrice {
	
}

// returns a table with all entries whose name match the search
search {
	
}